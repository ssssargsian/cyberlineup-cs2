services:
  db:
    image: postgres:16-alpine
    container_name: cs2-lineups-assistant-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: cyberlineup_sr
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
